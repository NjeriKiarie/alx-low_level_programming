#include "main.h"

/**
 * _islower - a function that checks for lowerecase characters
 * @c: the character
 * Return: 1 if c islowercase  and 0 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
